# Changelog

All notable changes to this project are documented in this file.

The format is inspired by Keep a Changelog.

## [0.1.0] - 2026-02-20
### Added
- Initial shared `yai-skin` repository extracted from YX skin assets.
- Governance baseline files (`VERSION`, `VERSIONING.md`, `CONTRIBUTING.md`, `SECURITY.md`, `CODE_OF_CONDUCT.md`, `NOTICE`).
- Lightweight docs set under `docs/`.
- Minimal `package.json` for future package publishing.

### Changed
- Clarified `index.css` as the single public entrypoint.
- Reordered CSS imports in `index.css` to follow layer policy.
