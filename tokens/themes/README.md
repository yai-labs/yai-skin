# Themes

Theme files override semantic tokens for a specific visual mode.

## Files

- `theme-dark.css`
- `theme-yai.css`

## Rules

- Keep themes limited to token overrides.
- Avoid component/layout selectors in theme files.
- New themes should not break existing semantic token keys.
