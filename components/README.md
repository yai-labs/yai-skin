# Components Layer

Reusable UI component styles.

## Files

- `buttons.css`
- `cards.css`
- `forms.css`
- `modal.css`

## Rules

- Components can consume tokens (`var(--...)`) but should not redefine token values.
- Keep selectors scoped and predictable.
- Prefer additive variants over breaking selector rewrites.
